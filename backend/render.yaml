services:
  - type: web
    name: citizen-fraud-shield-api
    env: python
    buildCommand: |
      apt-get update && apt-get install -y tesseract-ocr libtesseract-dev tesseract-ocr-eng || true
      pip install --no-cache-dir -r requirements.txt
    startCommand: python run.py